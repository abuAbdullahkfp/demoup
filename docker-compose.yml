version: '3'

services:
  scalehosterapp:
    build:
      context: ./categoryservice
    ports:
      - "3000:3000"
  assetservice:
    build:
      context: ./assetservice
    ports:
      - "3001:3001"
    depends_on:
      - mongodb
    volumes:
      - ./assetservice:/app
  mongodb:
    image: mongo 
    ports:
      - "27017:27017"

  